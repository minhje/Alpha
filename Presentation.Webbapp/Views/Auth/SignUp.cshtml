
@model SignUpViewModel

@{
    Layout = "_CleanLayout";
    ViewData["Title"] = "Create Account";
}

<div class="center-screen">
    <div id="signup" class="surface">

        <form asp-controller="Auth" asp-action="SignUp" method="post" novalidate enctype="multipart/form-data">
            <h1>@ViewData["Title"]</h1>

            @if (!string.IsNullOrEmpty(ViewBag?.ErrorMessage))
            {
                <div class="error validation-error">
                    @ViewBag?.ErrorMessage
                </div>
            }

            <div class="form-group">
                <label asp-for="FirstName"></label>
                <div class="field-group">
                    <input asp-for="FirstName" >
                    <span asp-validation-for="FirstName"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="LastName"></label>
                <div class="field-group">
                    <input asp-for="LastName" >
                    <span asp-validation-for="LastName"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Email"></label>
                <div class="field-group">
                    <input asp-for="Email" >
                    <span asp-validation-for="Email"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Password"></label>
                <div class="field-group">
                    <input asp-for="Password" >
                    <span asp-validation-for="Password"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="ConfirmPassword"></label>
                <div class="field-group">
                    <input asp-for="ConfirmPassword" />
                    <span asp-validation-for="ConfirmPassword"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="TermsAndConditions"></label>
                <div class="field-group">
                    <input asp-for="TermsAndConditions" >
                    <span asp-validation-for="TermsAndConditions"></span>
                </div>
            </div>

            <div class="form-group">
                <button type="submit" class="btn btn-submit btn-blue">Create Account</button>
            </div>

            <div class="form-footer">
                <span>Already have an account?</span>
                <a asp-controller="Auth" asp-action="Login">Login</a>
            </div>
        </form>


    </div>
</div>